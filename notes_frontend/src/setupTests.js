/**
 * Jest setup file.
 *
 * This template previously imported `@testing-library/jest-dom`, but the project
 * does not currently include a Jest test suite and we want to avoid pulling in
 * extra test-only dependencies and configuration.
 */
